{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.appName }}-ingress
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.appName }}
    env: {{ .Values.environment }}
  annotations:
    # AWS Load Balancer Controller annotations
    {{- if .Values.ingress.group.enabled }}
    {{- if eq .Values.ingress.className "alb" }}
    alb.ingress.kubernetes.io/target-type: {{ .Values.ingress.annotations.awsTargetType | default "ip" | quote }}
    alb.ingress.kubernetes.io/backend-protocol: {{ .Values.ingress.annotations.awsBackendProtocol | default "HTTP" | quote }}
    alb.ingress.kubernetes.io/success-codes: {{ .Values.ingress.annotations.awsSuccessCodes | default "200" | quote }}
    alb.ingress.kubernetes.io/healthcheck-path: {{ .Values.ingress.annotations.awsHealthcheckPath | default "/" | quote }}
    {{- end }}
    # AWS ALB Group configuration
    {{- if .Values.ingress.annotations.awsGroupName }}
    alb.ingress.kubernetes.io/group.name: {{ .Values.ingress.annotations.awsGroupName | quote }}
    {{- end }}
    {{- if .Values.ingress.annotations.awsListenPorts }}
    alb.ingress.kubernetes.io/listen-ports: {{ .Values.ingress.annotations.awsListenPorts | quote }}
    {{- end }}
    {{- end }}

    # Custom annotations adicionales
    {{- range $key, $value := .Values.ingress.annotations.custom }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}

  rules:
    {{- range .Values.ingress.rules }}
    - {{- if .host }}
      host: {{ .host }}
      {{- end }}
      http:
        paths:
        {{- range .paths }}
        - path: {{ .path }}
          pathType: {{ .pathType }}
          backend:
            service:
              name: {{ $.Values.appName }}-svc
              port:
                number: {{ .servicePort }}
        {{- end }}
    {{- end }}
{{- end }}
